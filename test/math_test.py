# from core import NumericSeries, BoolSeries
# from funcs import barslast, REF
# import numpy as np
#
#
# # arr = BoolSeries(np.array([False, False,True, False, False, True, False]))
# arr = BoolSeries(np.array([True, False, False, False]))
# # arr = BoolSeries(np.array([False, False, False,True,False,False]))
# # arr = BoolSeries(np.array([False, False, True, False, False, True, False]))
# result = barslast(arr)
# print(result.series)
#
# # print([1,2]==[1,2])
#
# # arr = NumericSeries(np.array([1,2,3,4,5]))
# # day = NumericSeries(np.array([2,2,2,2,2]))
# # result = REF(arr, 2)
# pass
